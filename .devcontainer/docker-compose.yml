version: '3'

services:

   wkspace:
      build:
         context: i/wkspace
         dockerfile: Dockerfile
         args:
            USERNAME: ${USERNAME}
            UPASSWD: ${UPASSWD}
      env_file: .env
      volumes:
         - wkspace:/wkspace
networks:
   frontend:
   backend:

volumes:
   wkspace:
